

class ConfigurationManager(object):
    """
    This is going to manage the configurations in the system.

    Can resolve configurations from file-based, and update them according to some
    requests.

    Configuration backends

    
    
    """

    def _run(self):
        pass
